{
  "name": "@stackradar/cluster-scanner",
  "//version": "Placeholder — stamped by CI from the release tag. See RELEASING.md.",
  "version": "0.0.0-dev",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.21.0",
  "scripts": {
    "dev": "LOG_PRETTY=true tsx src/watch.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit && tsc -p tsconfig.build.json",
    "start": "node dist/src/watch.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@kubernetes/client-node": "^1.0.0",
    "pino": "^10.3.1",
    "undici": "^8.10.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "pino-pretty": "^13.1.3",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0",
    "vitest": "^4.1.2"
  }
}
